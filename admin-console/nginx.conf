server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: ogni route non-file torna a index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
